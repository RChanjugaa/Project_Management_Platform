{
  "name": "project-team-management-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Project and team task management platform practical assignment.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "type-check": "npm run type-check --prefix client && npm run type-check --prefix server",
    "test": "npm run test --prefix server",
    "test:e2e:video": "node e2e/record-full-flow.cjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "selenium-webdriver": "^4.35.0"
  }
}
